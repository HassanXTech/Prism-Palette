export interface ColorPalette {
  name: string;
  baseColor: string;
  backgroundColor: string;
  lightShadow: string;
  darkShadow: string;
  highlight: string;
  textPrimary: string;
  textSecondary: string;
}
